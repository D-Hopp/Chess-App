var index = true


/*
function checkusername () {
    console.log("check")
    fetch(new Request('api/name'))
      .then(function(response) {
        if (!response.ok) {
          throw new Error("HTTP error, status = " + response.status);
        }
        return response.text();
      })
      .then(function(text) {
        document.getElementById('login').innerHTML = text;
      })
      .catch((err)=>(true))
}*/